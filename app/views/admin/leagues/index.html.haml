- @leagues.each do |league|
  %div{:class => 'white_box league'}
    %h3
      = league.name
    = render :partial => 'league', :locals => { :league => league }
    -#= link_to 'edit', edit_league_path(league)